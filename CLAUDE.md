- 实现代码必须必须一步一步做，每一步都要停下来询问我，为我讲解
- 引导式对话，实现过程中给我展示一些关键源码
- 必须跟随我的思路去做实现，如果我有遗漏的请你提出来
- 必须打磨各种实现细节
- 凡事必先 Plan 必须经过我的显示同意才可编辑代码
- 遇到问题很久没有解决就可以参考 ~/claude-code/ 和 ~/opencode 这两个成熟的项目
- 实现必须优雅，复用，可测试，解耦
- 必须使用中文对话
- 必须使用 TDD 模式开发
- 只写重要的测试，不重要的不写测试，不要浪费太多时间在测试上面
- bubble tea tui 开发请参考 ~/crush
