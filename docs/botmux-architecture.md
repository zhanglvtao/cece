# botmux 架构图

```
┌─────────────────────────────────────────────────────────────────────┐
│                          飞 书 云 端                                │
│                                                                     │
│  ┌──────────┐    ┌──────────┐    ┌──────────────────────────────┐  │
│  │ 话题群    │    │ 私聊     │    │ 飞书开放平台                  │  │
│  │ (topic)   │    │ (p2p)    │    │  - 事件订阅 (长连接)          │  │
│  │           │    │          │    │  - 消息 API                   │  │
│  │ @机器人   │    │ 直接发   │    │  - 卡片回传 (card.action)     │  │
│  │ 发消息    │    │ 消息     │    │  - 图片/文件上传              │  │
│  └────┬─────┘    └────┬─────┘    └──────────────┬───────────────┘  │
│       │               │                         │                  │
└───────┼───────────────┼─────────────────────────┼──────────────────┘
        │               │                         │
        │  WebSocket 长连接 (im.message.receive_v1 / card.action.trigger)
        │               │                         │
        ▼               ▼                         ▼
┌─────────────────────────────────────────────────────────────────────┐
│                      你 的 机 器 (开发机 / 服务器)                   │
│                                                                     │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │                     botmux Daemon (常驻进程)                   │  │
│  │                                                               │  │
│  │  ┌─────────────┐  ┌──────────────┐  ┌────────────────────┐   │  │
│  │  │ event-       │  │ command-     │  │ scheduler          │   │  │
│  │  │ dispatcher   │  │ handler      │  │ (定时任务 cron)    │   │  │
│  │  │             │  │              │  │                    │   │  │
│  │  │ · 消息路由  │  │ · /close     │  │ · /schedule add    │   │  │
│  │  │ · @mention  │  │ · /restart   │  │ · 自然语言解析    │   │  │
│  │  │ · 权限门控  │  │ · /repo      │  │ · 到点自动触发    │   │  │
│  │  │ · scope判定 │  │ · /adopt     │  │                    │   │  │
│  │  └──────┬──────┘  └──────┬───────┘  └────────┬───────────┘   │  │
│  │         │                │                    │               │  │
│  │         └────────────────┼────────────────────┘               │  │
│  │                          │                                    │  │
│  │         ┌────────────────▼──────────────────┐                 │  │
│  │         │  activeSessions Map                │                 │  │
│  │         │  key: anchor + larkAppId           │                 │  │
│  │         │  val: DaemonSession {              │                 │  │
│  │         │    session, worker, chatId,        │                 │  │
│  │         │    scope, streamCardId, ...        │                 │  │
│  │         │  }                                 │                 │  │
│  │         └────────┬──────────────────────────┘                 │  │
│  │                  │                                            │  │
│  │                  │ fork() / IPC (child_process.send)          │  │
│  │                  │                                            │  │
│  └──────────────────┼────────────────────────────────────────────┘  │
│                     │                                               │
│     ┌───────────────┼───────────────┬───────────────┐               │
│     │               │               │               │               │
│     ▼               ▼               ▼               ▼               │
│  ┌────────┐   ┌────────┐   ┌────────┐   ┌────────┐               │
│  │Worker 1│   │Worker 2│   │Worker 3│   │Worker 4│  ...           │
│  │(fork'd)│   │(fork'd)│   │(fork'd)│   │(fork'd)│               │
│  │        │   │        │   │        │   │        │               │
│  │  ┌───┐ │   │  ┌───┐ │   │  ┌───┐ │   │  ┌───┐ │               │
│  │  │PTY│ │   │  │PTY│ │   │  │PTY│ │   │  │PTY│ │               │
│  │  │ or│ │   │  │ or│ │   │  │ or│ │   │  │ or│ │               │
│  │  │tmx│ │   │  │tmx│ │   │  │tmx│ │   │  │tmx│ │               │
│  │  └─┬─┘ │   │  └─┬─┘ │   │  └─┬─┘ │   │  └─┬─┘ │               │
│  │    │   │   │    │   │   │    │   │   │    │   │               │
│  │    ▼   │   │    ▼   │   │    ▼   │   │    ▼   │               │
│  │ ┌────┐ │   │ ┌────┐ │   │ ┌────┐ │   │ ┌────┐ │               │
│  │ │CC  │ │   │ │Codex│ │   │ │CoCo│ │   │ │Gem.│ │               │
│  │ └──┬─┘ │   │ └──┬─┘ │   │ └──┬─┘ │   │ └──┬─┘ │               │
│  └────┼───┘   └────┼───┘   └────┼───┘   └────┼───┘               │
│       │            │            │            │                     │
│       ▼            ▼            ▼            ▼                     │
│  ┌────────────────────────────────────────────────────┐            │
│  │              Worker 内部组件                         │            │
│  │                                                    │            │
│  │  headless xterm ──→ TerminalRenderer ──→ 飞书卡片   │            │
│  │  (渲染终端输出)     (去装饰 + 截图)     (流式 PATCH)  │            │
│  │                                                    │            │
│  │  fs.watch / poll ──→ Bridge Fallback ──→ 飞书消息   │            │
│  │  (监控 JSONL)        (转录兜底)         (final_output)│           │
│  │                                                    │            │
│  │  HTTP/WS 服务器 ──→ Web 终端 (xterm.js)             │            │
│  │  (可交互)           (手机/电脑浏览器打开)             │            │
│  │                                                    │            │
│  │  ScreenAnalyzer ──→ TUI 交互提示检测                │            │
│  │  (AI 视觉识别)       (如 AskUserQuestion)            │            │
│  └────────────────────────────────────────────────────┘            │
│                                                                     │
│  ┌────────────────────────────────────────────────────┐            │
│  │              转录文件 (Bridge Fallback 数据源)       │            │
│  │                                                    │            │
│  │  ~/.claude/projects/.../<sid>.jsonl    (Claude)    │            │
│  │  ~/.codex/sessions/.../rollout-xx.jsonl (Codex)   │            │
│  │  ~/.cache/coco/sessions/<sid>/events.jsonl (CoCo) │            │
│  └────────────────────────────────────────────────────┘            │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘

                        ┌──────────────────┐
                        │  消 息 流 向       │
                        ├──────────────────┤
                        │                  │
                        │  用户发消息       │
                        │    │             │
                        │    ▼             │
                        │  飞书 → Daemon   │
                        │    │ (WS 推送)    │
                        │    ▼             │
                        │  Worker → PTY    │
                        │    │ (writeInput) │
                        │    ▼             │
                        │  CLI Agent       │
                        │    │             │
                        │    ├──→ 终端输出 (所有)                     │
                        │    │     headless xterm → 流式卡片           │
                        │    │                                        │
                        │    ├──→ botmux send (模型主动)               │
                        │    │     飞书 API → 话题回复                 │
                        │    │                                        │
                        │    └──→ JSONL 转录 (被动兜底)                │
                        │          Bridge Fallback → 飞书 API         │
                        │                  │
                        └──────────────────┘
```